# 回归资产清单（历史事故 → 测试覆盖）

> F-03 交付物：从 git log / 交接文档收集的历史事故，逐一确认回归测试覆盖。
> 覆盖判定标准：测试存在 + 断言直接针对事故行为（失败即事故复现）。
> 本清单与 `ci/script/check_migration_coverage.py`（F-02 迁移链硬校验）配套，
> 新增迁移必须同步更新测试，否则 CI 失败。

## 事故清单与覆盖矩阵

| # | 事故 | 回归测试文件 | 关键断言 |
|---|------|-------------|---------|
| 1 | **迁移断链**（某版本无出发迁移，升级路径不可达） | `MuseDbMigrationTest.kt` + `MuseDbManualChainMigrationTest.kt` + `ci/script/check_migration_coverage.py` | 全链逐步迁移断言列/索引/数据保留；ManualChain 反射枚举 22..54 起点全链执行；F-02 脚本硬校验 from 覆盖 1..N-1（删 MIGRATION_89_90/90_91 场景必 FAIL） |
| 2 | **生图 null 假图**（Provider 返回空/全 null 图，UI 渲染假图占位） | `ImageServiceEmptyResultTest.kt`（新增，ai 模块） | ①空结果抛 `IMAGE_NO_RESULTS` 而非返回空列表（ImageService.kt:107）；②`convertToOutputStrings` mapNotNull 过滤 base64/url 全 null 的 GeneratedImage（ImageService.kt:219）；③非空结果原样映射输出 URL |
| 3 | **finalAssistantMessage 媒体丢失**（合并最终消息时 imageUrls/videoFileUri/imageBase64List 丢失） | `ChatMediaMergeTest.kt` | 合并结果 imageUrls/imageBase64List/videoFileUri 逐项 assertEquals；空媒体断言 emptyList/assertNull；非媒体字段不受影响 |
| 4 | **空流回退**（首事件看门狗：流无响应时 FallbackNotice + 回退回复，禁止混入正常内容） | `FirstEventWatchdogTest.kt` | 空流场景断言 FallbackNotice 出现 + ContentDelta=="fallback reply" + 末事件 Done；正常流场景断言 FallbackNotice 不出现且回退文案不出现（否定断言） |
| 5 | **主动消息 JSON 截断**（配置序列化往返丢字段/兼容旧配置） | `ProactiveMessageConfigTest.kt` | roundTripKeepsNextTriggerAt / roundTripKeepsSendProbability（往返保字段）；oldConfigWithoutProbabilityDecodesAsDefault（旧配置兼容默认值） |
| 6 | **头像重启丢失**（实体序列化往返丢字段） | `AssistantEntitySerializationTest.kt` | entity 序列化往返 assertEquals(entity, decoded)；非法输入 assertThrows(SerializationException) |

## 各事故的代码防护位置（供回归定位）

1. **迁移断链**：`MuseDb.kt` version=91，addMigrations 全链 1→91（含 68_74 直跳 + 动态 migrate75To76/migrate76To77）。防护=Room Migration 精确匹配 + F-02 脚本。
2. **生图 null 假图**：`ai/.../image/ImageService.kt:107`（空结果抛错）+ `:219`（mapNotNull 过滤）+ `app/.../ui/chat/ImageGenCoordinator.kt:355`（协调层 urls.isEmpty() 兜底报 err_image_gen_no_result）。
3. **finalAssistantMessage 媒体丢失**：`ui/chat/ChatMediaMerge.kt`（合并逻辑）。
4. **空流回退**：`ai/.../core/` 流看门狗（FirstEventWatchdog），FallbackNotice 事件。
5. **主动消息 JSON 截断**：`schedule/ProactiveMessageConfig.kt`（@Serializable 往返）。
6. **头像重启丢失**：`assistant/AssistantEntity.kt`（@Serializable 往返）。

## 覆盖缺口与补测记录

- 事故 2（生图 null 假图）**原无回归测试**（ImageServicePureTest 仅测 tick 检查逻辑）→ 本轮新增
  `ai/src/test/java/io/zer0/ai/image/ImageServiceEmptyResultTest.kt`（3 个测试：空结果抛错 / null 图过滤 / 非空映射）。
- 其余 5 类事故均确认已有测试覆盖，无需补测。

## 维护约定

- 新增/修改数据库迁移：必须同步更新迁移测试（MuseDbMigrationTest 全链段或 ManualChain），
  并保持 F-02 脚本通过（`python3 ci/script/check_migration_coverage.py`）。
- 新增/修改生图链路：必须保持 ImageServiceEmptyResultTest 通过（空结果必须抛错）。
- 本清单随事故库更新：新事故复盘后补一行 + 对应测试。
