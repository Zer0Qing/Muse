# Phase 3 — `runCatching` 调用点分类清单

> 生成时间: 2026-07-28
> 扫描范围: `e:\1Project\Muse\1muse` 下所有 `.kt` 文件(排除 `build/`、`docs/`、`.kt.txt`)
> 分类规则:
> - **HIGH** (高危): 块内调用 suspend 函数(可能吞 `CancellationException`),或所在函数是 `suspend fun` / 协程作用域且块内有 IO/网络等可中断操作
> - **FALLBACK** (降级回退): 块内是纯同步逻辑,调用方明确意图是"失败则用默认值/空值降级"
> - **THROW** (可上抛): 使用 `getOrThrow()` 或 `getOrNull()` 后立即 throw / return error
> - **UNKNOWN**: 难以判断类别
>
> **分类策略说明**: 当 `runCatching` 位于 `suspend fun` / 协程作用域内但块内仅包含纯同步逻辑(如 JSON 解析、枚举 valueOf、日期解析)且使用 `getOrNull/getOrElse/getOrDefault` 降级时,归类为 FALLBACK(因 `CancellationException` 只在 suspend 挂起点抛出,纯同步块无此风险)。仅当块内确实调用 suspend 函数或包含 IO 等可被中断的长操作时才标 HIGH。

---

## :ai 模块

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\video\VideoProviderRegistry.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 58 | FALLBACK | `runCatching { java.net.URI(config.resolvedBaseUrl()).host?.lowercase() }.getOrNull().orEmpty()` — 同步 URI host 解析,失败降级为空串 |
| 94 | FALLBACK | `runCatching { java.net.URI(baseUrl).host?.lowercase() ?: "" }.getOrElse { ... }` — 同步 URI 解析,失败降级为 substringAfter 兜底 |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\video\VideoGenerationService.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 64 | HIGH | `runCatching { val provider = ...; provider.submit(requestWithCreds) }` — 在 `withContext(Dispatchers.IO)` suspend 函数内,`provider.submit` 是 suspend 调用 |
| 92 | — | `return@runCatching url` — 非调用点,是 runCatching 块内的 return 标签 |
| 114 | HIGH | `runCatching { val provider = ...; provider.submit(requestWithCreds) }` — 同上,在 suspend 函数 `submitRequest` 的 `withContext` 内 |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\video\KlingVideoProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 85 | HIGH | `runCatching { ...; exec(httpRequest).use { ... } }` — 在 `override suspend fun submit` 的 `withContext(Dispatchers.IO)` 内,`exec` 是 suspend 函数 |
| 148 | HIGH | `runCatching { ...; exec(httpRequest).use { ... } }` — 在 `override suspend fun poll` 的 `withContext(Dispatchers.IO)` 内,`exec` 是 suspend 函数 |
| 271 | FALLBACK | `runCatching { json.parseToJsonElement(body).jsonObject... }.getOrNull()` — 同步 JSON 解析,失败返回 null |
| 294 | FALLBACK | `cont.invokeOnCancellation { runCatching { call.cancel() } }` — 协程取消时清理 OkHttp Call,纯同步且忽略失败 |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\video\GenericOpenAiVideoProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 80 | HIGH | `runCatching { ...; exec(httpRequest) }` — 在 `override suspend fun submit` 的 `withContext(Dispatchers.IO)` 内,`exec` 是 suspend |
| 158 | HIGH | `runCatching { ...; exec(httpRequest) }` — 在 `override suspend fun poll` 的 `withContext(Dispatchers.IO)` 内,`exec` 是 suspend |
| 290 | FALLBACK | `runCatching { json.parseToJsonElement(body)... }.getOrNull()` — 同步 JSON 解析 |
| 308 | FALLBACK | `cont.invokeOnCancellation { runCatching { call.cancel() } }` — 清理 OkHttp Call |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\video\AgnesVideoProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 90 | HIGH | `runCatching { ...; exec(httpRequest) }` — 在 `override suspend fun submit` 的 `withContext(Dispatchers.IO)` 内,`exec` 是 suspend |
| 186 | HIGH | `runCatching { ...; exec(httpRequest) }` — 在 `override suspend fun poll` 的 `withContext(Dispatchers.IO)` 内,`exec` 是 suspend |
| 328 | FALLBACK | `runCatching { json.parseToJsonElement(body)... }.getOrNull()` — 同步 JSON 解析 |
| 373 | FALLBACK | `cont.invokeOnCancellation { runCatching { call.cancel() } }` — 清理 OkHttp Call |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\image\OpenAIImageProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 333 | FALLBACK | `cont.invokeOnCancellation { runCatching { call.cancel() } }` — 清理 OkHttp Call |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\image\ImageProviderRegistry.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 55 | FALLBACK | `runCatching { java.net.URI(config.resolvedBaseUrl()).host?.lowercase() }.getOrNull().orEmpty()` — 同步 URI 解析,降级为空串 |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\image\AgnesImageProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 181 | FALLBACK | `cont.invokeOnCancellation { runCatching { call.cancel() } }` — 清理 OkHttp Call |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\plugin\ProviderPluginRegistry.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 52 | FALLBACK | `runCatching { if (dir.exists()...) dir.listFiles()... }` — init 块同步文件扫描,失败不影响主流程 |
| 56 | FALLBACK | `runCatching { json.decodeFromString<ProviderPlugin>(file.readText()); validate(plugin) }` — 单插件文件加载,失败仅记日志跳过 |
| 115 | HIGH | `runCatching { file.readText(); ...; register(plugin) }` — 在 `suspend fun loadFromFile` 的 `withContext(Dispatchers.IO)` 内,`register` 可能触发 IO |
| 190 | FALLBACK | `runCatching { dir.mkdirs(); file.writeText(...) }.onFailure { Logger.e(...) }` — 同步文件写入,失败仅记日志 |
| 202 | FALLBACK | `runCatching { file.delete() }.onFailure { Logger.e(...) }` — 同步文件删除,失败仅记日志 |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\core\ProviderError.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 246 | FALLBACK | `runCatching { inferFromMessage(message, throwable) }.getOrElse { null }` — 同步错误推断,失败返回 null |

### `e:\1Project\Muse\1muse\ai\src\main\java\io\zer0\ai\core\ProviderHttpSupport.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 235 | FALLBACK | `runCatching { sharedHttpClient.connectionPool.evictAll() }.onFailure { Logger.w(...) }` — 同步连接池清理,失败仅记日志 |

---

## :memory 模块

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\deep\DeepMemoryProcessor.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 251 | FALLBACK | `runCatching { json.decodeFromString(ListSerializer(FactDto.serializer()), s) }.getOrElse { ... return emptyList() }` — 同步 JSON 解析,失败降级为空列表 |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\pin\PinnedMemoryStore.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 125 | FALLBACK | `runCatching { json.decodeFromString(...) }.getOrElse { emptyList() }` — 同步 JSON 解析,失败降级为空列表 |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\budget\LlmBudget.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 28 | FALLBACK | `runCatching { Encodings.newDefaultEncodingRegistry().getEncoding(...) }.getOrNull()` — 同步 encoding 初始化,失败返回 null |
| 81 | FALLBACK | `runCatching { enc.countTokens(text) }.getOrDefault(-1)` — 同步 token 计数,失败降级为 -1 |
| 85 | FALLBACK | `runCatching { enc.encode(text); ...; enc.decode(truncated) }.getOrNull()` — 同步 BPE 编解码,失败返回 null |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\compile\MemoryFileWriter.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 50 | FALLBACK | `runCatching { File(memoryDir, "memory.md").writeText(content) }.onFailure { Logger.w(...) }` — 同步文件写入,失败仅记日志 |
| 60 | FALLBACK | `runCatching { File(memoryDir, "memory.md").let { if (it.exists()) it.readText() else null } }.getOrElse { null }` — 同步文件读取,失败降级为 null |
| 75 | FALLBACK | `runCatching { File(dailyDir, "$date.md").writeText(...) }.onFailure { Logger.w(...) }` — 同步文件写入 |
| 90 | FALLBACK | `runCatching { File(dailyDir, "$date.md").let { if (!it.exists()) "" else normalizeDailyBody(it.readText()) } }.getOrElse { "" }` — 同步文件读取,降级为空串 |
| 105 | FALLBACK | `runCatching { dailyDir.listFiles(...)... }.getOrElse { emptyList() }` — 同步文件列表,降级为空列表 |
| 135 | FALLBACK | `runCatching { listRecentDailyEntries(maxDays)... }.getOrElse { "" }` — 同步文件组装,降级为空串 |
| 172 | FALLBACK | `runCatching { ... }.getOrElse { RollResult(emptyList(), emptyList(), "") }` — 同步文件滚动窗口,降级为空结果 |
| 173 | FALLBACK | `runCatching { LocalDate.parse(referenceDate).minusDays(...) }.getOrElse { ... return@runCatching RollResult(...) }` — 同步日期解析,降级为空结果 |
| 192 | FALLBACK | `entries.forEach { runCatching { it.file.delete() } }` — 同步文件删除,忽略失败 |
| 206 | FALLBACK | `runCatching { File(dailyDir, "$date.md").delete() }.onFailure { Logger.w(...) }` — 同步文件删除 |
| 216 | FALLBACK | `runCatching { File(dailyDir, "$date.md").delete(); File(...fingerprint).delete() }.onFailure { Logger.w(...) }` — 同步文件删除 |
| 225 | FALLBACK | `runCatching { File(dailyDir, "$date.md.fingerprint").let { if (it.exists()) it.readText().trim() else null } }.getOrElse { null }` — 同步文件读取,降级为 null |
| 235 | FALLBACK | `runCatching { File(dailyDir, "$date.md.fingerprint").writeText(fingerprint) }.onFailure { Logger.w(...) }` — 同步文件写入 |
| 244 | FALLBACK | `runCatching { File(dailyDir, "$date.md.fingerprint").delete() }.onFailure { Logger.w(...) }` — 同步文件删除 |
| 253 | FALLBACK | `runCatching { dailyDir.listFiles(...)?.forEach { it.delete() } }.onFailure { Logger.w(...) }` — 同步文件清空 |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\compile\MemoryCompiler.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 150 | FALLBACK | `runCatching { LocalDate.parse(logicalDate) }.getOrNull()` — 同步日期解析,失败返回 null |
| 513 | FALLBACK | `runCatching { Instant.parse(updatedAtIso) }.getOrNull() ?: return 0f` — 同步时间戳解析,失败返回 0f |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\fact\FactDbProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 50 | FALLBACK | `cache.values.forEach { runCatching { it.close() } }` — 同步数据库关闭,忽略失败 |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\time\TimeContext.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 42 | FALLBACK | `runCatching { ZoneId.of(raw ?: DEFAULT_TIMEZONE) }.getOrElse { ZoneId.of(DEFAULT_TIMEZONE) }` — 同步时区解析,降级为默认时区 |
| 110 | FALLBACK | `runCatching { Instant.parse(value) }.recoverCatching { LocalDate.parse(value)... }.getOrNull()` — 同步时间戳解析,失败返回 null |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\fact\FactStore.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 316 | HIGH | `runCatching { dao.searchFts(matchQuery, limit) }.getOrElse { emptyList() }` — 在 `suspend fun searchFullText` 的 `withContext(Dispatchers.IO)` 内,`dao.searchFts` 是 suspend DAO 调用 |
| 439 | HIGH | `runCatching { dao.countFacts() }.getOrDefault(-1)` — 在 suspend 函数内,`dao.countFacts` 是 suspend DAO 调用 |
| 440 | HIGH | `runCatching { dao.countFts() }.getOrDefault(-1)` — 在 suspend 函数内,`dao.countFts` 是 suspend DAO 调用 |
| 463 | HIGH | `runCatching { dao.insertFts(row.id, FactFtsManager.toNgram(row.fact)) }.onFailure { ... }` — 在 suspend 函数内,`dao.insertFts` 是 suspend DAO 调用 |
| 579 | FALLBACK | `runCatching { json.decodeFromString(ListSerializer(String.serializer()), raw) }.getOrElse { emptyList() }` — 同步 JSON 解析,降级为空列表 |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\ticker\MemoryTicker.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 242 | FALLBACK | `runCatching { Instant.parse(value).toString() }.getOrNull()` — 同步时间戳解析,失败返回 null |
| 283 | FALLBACK | `runCatching { json.decodeFromString(stepsSerializer, state.completedSteps) }.getOrElse { emptyMap() }` — 同步 JSON 解析,降级为空 map |
| 366 | FALLBACK | `runCatching { LocalDate.parse(context.logicalDate).minusDays(1).toString() }.getOrNull()` — 同步日期解析,失败返回 null |

### `e:\1Project\Muse\1muse\memory\src\main\java\io\zer0\memory\summary\SessionSummaryManager.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 299 | FALLBACK | `runCatching { json.decodeFromString(TimeContext.SourceTimeRange.serializer(), it) }.getOrElse { null }` — 同步 JSON 解析,失败返回 null |

---

## :app 模块

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\AppKoinModule.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 375 | HIGH | `runCatching { ... }` — 在 suspend 上下文内,需检查块内是否调用 suspend 函数(块内读取首条 chunk 内容,可能涉及 suspend 调用) |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\MuseApp.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 236 | — | 注释 `// M1: 统一用 resultOf{} 替代 runCatching{}` — 非调用点 |
| 433 | — | 注释 — 非调用点 |
| 535 | — | 注释 — 非调用点 |
| 562 | — | 注释 — 非调用点 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\auth\SecureCredentialStore.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 80 | FALLBACK | `runCatching { ... }` — 同步加密存储操作,失败降级处理 |
| 142 | FALLBACK | `runCatching { ... }` — 同步解密读取操作,失败降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\auth\OAuthManager.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 174 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 308 | FALLBACK | `runCatching { activity.startActivity(intent) }.onFailure { ... }` — 同步启动 Activity,失败仅记日志 |
| 477 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 511 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 543 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\backup\webdav\WebDavClient.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 187 | HIGH | `runCatching { ... }` — 在 suspend 函数内,WebDAV HTTP 操作可能包含 suspend 调用 |
| 263 | FALLBACK | `runCatching { java.net.URLDecoder.decode(href, "UTF-8") }.getOrDefault(href)` — 同步 URL 解码,失败降级为原串 |
| 284 | HIGH | `runCatching { ... }` — 在 suspend 函数内,WebDAV HTTP 操作 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\backup\BackupCrypto.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 71 | FALLBACK | `runCatching { String(data, 0, MAGIC.length, Charsets.US_ASCII) }.getOrNull()` — 同步字节转串,失败返回 null |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\backup\s3\S3Client.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 148 | HIGH | `runCatching { ... }` — 在 suspend 函数内,S3 HTTP 操作 |
| 332 | FALLBACK | `runCatching { isoFmt.parse(text)?.time }.recoverCatching { isoFmtNoMs.parse(text)?.time }.getOrNull() ?: 0L` — 同步日期解析,降级为 0L |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\doc\OcrManager.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 77 | FALLBACK | `runCatching { ... }` — 同步 OCR 初始化/识别,失败降级 |
| 96 | FALLBACK | `runCatching { ... }` — 同步 OCR 操作,失败降级 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\doc\DocumentParser.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 171 | FALLBACK | `runCatching { decoder.decode(java.nio.ByteBuffer.wrap(bytes)); StandardCharsets.UTF_8 }.getOrElse { ... }` — 同步字符集检测,降级为 GBK/UTF-8 |
| 177 | FALLBACK | `runCatching { Charset.forName("GBK") }.getOrElse { StandardCharsets.UTF_8 }` — 同步字符集查找,降级为 UTF-8 |
| 217 | FALLBACK | `runCatching { stripper.getText(doc) }` — 同步 PDF 文本提取,失败降级 |
| 276 | FALLBACK | `runCatching { ... }` — 同步 PDF 元数据提取,降级 |
| 293 | FALLBACK | `runCatching { ... }` — 同步 PDF 大纲提取,降级 |
| 313 | FALLBACK | `runCatching { ... }` — 同步文档解析,降级 |
| 397 | FALLBACK | `runCatching { PDFBoxResourceLoader.init(context) }` — 同步 PDFBox 初始化,忽略失败 |
| 860 | FALLBACK | `runCatching { ... }` — 同步文档解析,降级 |
| 890 | FALLBACK | `runCatching { factory.setFeature("http://xml.org/sax/features/external-general-entities", false) }` — 同步 SAX 安全配置,忽略失败 |
| 891 | FALLBACK | `runCatching { factory.setFeature("http://xml.org/sax/features/external-parameter-entities", false) }` — 同步 SAX 安全配置,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\network\NetworkMonitor.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 76 | FALLBACK | `runCatching { ... }` — 同步网络状态检查,失败降级 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\mcp\McpClient.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 482 | FALLBACK | `runCatching { AppJson.decodeFromString(JsonObject.serializer(), data) }.getOrNull() ?: return` — 同步 JSON 解析,失败返回 |
| 521 | FALLBACK | `runCatching { AppJson.decodeFromString(JsonObject.serializer(), body) }.getOrNull()` — 同步 JSON 解析,在 suspend `withContext` 内但块内纯同步 |
| 542 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 614 | FALLBACK | `runCatching { AppJson.decodeFromString(JsonObject.serializer(), body) }.getOrNull()` — 同步 JSON 解析 |
| 619 | FALLBACK | `runCatching { AppJson.decodeFromString(JsonObject.serializer(), body) }.getOrNull()` — 同步 JSON 解析 |
| 643 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(emptyList())` — 同步 JSON 解析,降级为空列表 |
| 652 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(emptyList())` — 同步 JSON 解析,降级为空列表 |
| 717 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(emptyList())` — 同步 JSON 解析,在 suspend `withContext` 内但块内纯同步 |
| 748 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(McpToolCallResult(isError = true))` — 同步 JSON 解析,在 suspend `withContext` 内但块内纯同步 |
| 772 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(emptyList())` — 同步 JSON 解析,在 suspend `withContext` 内但块内纯同步 |
| 801 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(emptyList())` — 同步 JSON 解析,在 suspend `withContext` 内但块内纯同步 |
| 826 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(emptyList())` — 同步 JSON 解析,在 suspend `withContext` 内但块内纯同步 |
| 865 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrDefault(McpPromptResult())` — 同步 JSON 解析,在 suspend `withContext` 内但块内纯同步 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\mcp\McpRegistry.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 158 | FALLBACK | `runCatching { client.startOAuthFlow() }.getOrNull()` — 同步 OAuth 启动,失败返回 null |
| 409 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\mcp\McpOAuthFlow.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 301 | HIGH | `runCatching { ... }` — OAuth HTTP 请求,在 suspend 函数内 |
| 333 | FALLBACK | `runCatching { ... }.getOrNull()` — 同步 JSON 解析,失败返回 null |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\mcp\McpDynamicRegistration.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 246 | HIGH | `runCatching { ... }` — 在 suspend 函数内,MCP 动态注册 HTTP 请求 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\web\WebSearchService.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 140 | FALLBACK | `runCatching { json.parseToJsonElement(body) as? JsonObject }.getOrNull() ?: return emptyList()` — 同步 JSON 解析,失败返回空列表 |
| 218 | FALLBACK | `runCatching { json.parseToJsonElement(body) as? JsonObject }.getOrNull() ?: return emptyList()` — 同步 JSON 解析 |
| 599 | FALLBACK | `runCatching { ... }` — 同步 gzip 解压,失败降级 |
| 602 | FALLBACK | `runCatching { ... }` — 同步 deflate 解压,失败降级 |
| 684 | FALLBACK | `runCatching { ... }` — 同步 URL 清洗,降级为 "Perplexity Citation" |
| 753 | FALLBACK | `runCatching { json.parseToJsonElement(body) as? JsonObject }.getOrNull() ?: return emptyList()` — 同步 JSON 解析 |
| 822 | FALLBACK | 同上模式 — 同步 JSON 解析 |
| 884 | FALLBACK | 同上模式 — 同步 JSON 解析 |
| 951 | FALLBACK | 同上模式 — 同步 JSON 解析 |
| 1018 | FALLBACK | 同上模式 — 同步 JSON 解析 |
| 1087 | FALLBACK | 同上模式 — 同步 JSON 解析 |
| 1159 | FALLBACK | 同上模式 — 同步 JSON 解析 |
| 1310 | FALLBACK | 同上模式 — 同步 JSON 解析 |
| 1342 | FALLBACK | `runCatching { ... }` — 同步 URL host 提取,降级为 "Perplexity Citation" |
| 1479 | FALLBACK | `cont.invokeOnCancellation { runCatching { call.cancel() } }` — 清理 OkHttp Call |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\web\SearchRateLimiter.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 90 | FALLBACK | `runCatching { ... }` — 同步限流器操作,失败降级 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\schedule\GroupChatScheduler.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 222 | FALLBACK | `runCatching { ChatGenerationService.start(appContext) }` — 同步启动前台服务,失败仅记日志 |
| 262 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |
| 281 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |
| 319 | FALLBACK | `runCatching { ChatGenerationService.start(appContext) }` — 同步启动前台服务 |
| 331 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |
| 367 | FALLBACK | `runCatching { ChatGenerationService.start(appContext) }` — 同步启动前台服务 |
| 412 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |
| 449 | FALLBACK | `runCatching { ChatGenerationService.start(appContext) }` — 同步启动前台服务 |
| 469 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |
| 504 | FALLBACK | `runCatching { ChatGenerationService.start(appContext) }` — 同步启动前台服务 |
| 562 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\schedule\ProactiveMessageRunner.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 488 | FALLBACK | `runCatching { java.time.Instant.parse(fact.createdAt).toEpochMilli() > lastTriggeredAt }.getOrDefault(false)` — 在 suspend 函数内但块内纯同步 Instant 解析,降级为 false |
| 633 | FALLBACK | `runCatching { decisionJson.decodeFromString<List<String>>(keywordsJson) }.getOrDefault(emptyList())` — 同步 JSON 解析,降级为空列表 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\schedule\UserActivityProfile.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 170 | FALLBACK | `runCatching { ConversationEndType.valueOf(state.lastConversationEndType) }.getOrDefault(...)` — 同步枚举 valueOf,降级为默认值 |
| 247 | FALLBACK | `runCatching { ... }` — 同步解析,降级为默认值 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\schedule\ChatGenerationService.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 67 | FALLBACK | `runCatching { wakeLock?.acquire(10 * 60 * 1000L) }` — 同步 wakeLock 获取,忽略失败 |
| 123 | FALLBACK | `runCatching { wakeLock?.takeIf { it.isHeld }?.release() }` — 同步 wakeLock 释放,忽略失败 |
| 155 | FALLBACK | `runCatching { wakeLock?.takeIf { it.isHeld }?.release() }` — 同步 wakeLock 释放,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\tools\AgentCapability.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 89 | FALLBACK | `runCatching { ... }` — 同步解析,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\tools\DelegationContract.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 194 | FALLBACK | `runCatching { ... }` — 同步解析,降级处理 |
| 206 | FALLBACK | `runCatching { ... }` — 同步解析,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\tools\ToolOrchestrator.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 912 | FALLBACK | `runCatching { f.lastModified() }.getOrDefault(0L)` — 同步文件 mtime 读取,降级为 0L |
| 914 | FALLBACK | `runCatching { f.delete() }` — 同步文件删除,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\transformer\LorebookTransformer.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 65 | FALLBACK | `runCatching { mutableExtras["lorebook_injected_ids"] = set }` — 同步 map 写入,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\transformer\TemplateTransformer.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 137 | FALLBACK | `runCatching { engine.render(msg.content, vars) }.getOrElse { msg.content }` — 同步模板渲染,降级为原内容 |
| 156 | FALLBACK | `runCatching { ... }` — 同步模板渲染,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\transformer\SystemPromptAssembler.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 163 | FALLBACK | `runCatching { getExperiments() }.getOrDefault(ExperimentsConfig())` — 同步配置读取,降级为默认配置 |
| 220 | FALLBACK | `runCatching { getMultiAgentConfig() }.getOrDefault(MultiAgentConfig())` — 同步配置读取,降级为默认配置 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\transformer\ContextCompressTransformer.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | FALLBACK | (具体行号需确认)同步压缩操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\transformer\MemoryInjectionTransformer.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | FALLBACK | (具体行号需确认)同步记忆注入,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\vision\VisionImagePreprocessor.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 93 | FALLBACK | `runCatching { ... }` — 同步图像预处理,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\vision\VisionBridge.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 697 | HIGH | `runCatching { settings.visionModelIdFlow.first() }.getOrNull() ?: ""` — 在 suspend 函数内,`flow.first()` 是 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\rag\EmbeddingService.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | HIGH | (具体行号需确认)在 suspend 函数内,嵌入服务调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\rag\EmbeddingProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | HIGH | (具体行号需确认)在 suspend 函数内,嵌入调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\util\TokenEstimator.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | FALLBACK | (具体行号需确认)同步 token 估算,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\UpdateChecker.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | HIGH | (具体行号需确认)在 suspend 函数内,HTTP 更新检查 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\SettingsRepository.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 215 | FALLBACK | `runCatching { AppJson.decodeFromString(serializer, raw) }.getOrElse { ... }` — 同步 JSON 解析,降级为默认值 |
| 605 | FALLBACK | `runCatching { SessionPermissionMode.valueOf(name) }.getOrNull()` — 同步枚举 valueOf,失败返回 null |
| 1093 | FALLBACK | `runCatching { AppJson.decodeFromString(ProviderConfig.serializer(), legacy) }.onSuccess { ... }` — 同步 JSON 解析 |
| 1398 | HIGH | `runCatching { ... }` — 在 suspend 函数内,连接测试可能包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\SecureKeyStore.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | FALLBACK | (具体行号需确认)同步密钥存储,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\session\SessionRepository.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 758 | FALLBACK | `runCatching { Uuid.parse(id) }.getOrElse { ... }` — 同步 UUID 解析,降级为随机 UUID |
| 762 | FALLBACK | `runCatching { MessageRole.valueOf(role) }.getOrDefault(MessageRole.USER)` — 同步枚举 valueOf,降级为 USER |
| 820 | FALLBACK | `runCatching { json.encodeToString(urlListSerializer, urls) }.getOrDefault("[]")` — 同步 JSON 序列化,降级为 "[]" |
| 823 | FALLBACK | `runCatching { json.decodeFromString(urlListSerializer, s) }.getOrDefault(emptyList())` — 同步 JSON 解析,降级为空列表 |
| 827 | FALLBACK | `runCatching { json.encodeToString(ragCitationListSerializer, citations) }.getOrDefault("[]")` — 同步 JSON 序列化,降级为 "[]" |
| 830 | FALLBACK | `runCatching { json.decodeFromString(ragCitationListSerializer, s) }.getOrDefault(emptyList())` — 同步 JSON 解析,降级为空列表 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\session\FolderRepository.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 87 | HIGH | `runCatching { folderDao.incrementSessionCount(oldFolderId, -1) }` — 在 suspend 函数内,DAO 调用 |
| 92 | HIGH | `runCatching { folderDao.incrementSessionCount(folderId, 1) }` — 在 suspend 函数内,DAO 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\session\MessageImageStore.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 56 | FALLBACK | `runCatching { ... }` — 同步图片文件操作,降级处理 |
| 82 | FALLBACK | `runCatching { ... }` — 同步图片文件操作,降级处理 |
| 100 | FALLBACK | `runCatching { ... }` — 同步图片文件操作,降级处理 |
| 104 | FALLBACK | `runCatching { ... }` — 同步图片文件删除,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\assistant\AssistantRepository.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 205 | FALLBACK | `runCatching { AppJson.decodeFromString(ListSerializer(UIMessage.serializer()), entity.presetMessagesJson) }.getOrElse { emptyList() }` — 同步 JSON 解析,降级为空列表 |
| 260 | FALLBACK | `runCatching { AppJson.parseToJsonElement(raw)...; baseConfig.copy(...) }.getOrElse { baseConfig }` — 同步 JSON 解析,降级为 baseConfig |
| 303 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrElse { emptyMap() }` — 同步 JSON 解析,降级为空 map |
| 316 | FALLBACK | `runCatching { ReasoningLevel.valueOf(entity.reasoningLevel) }.getOrElse { ReasoningLevel.DEFAULT }` — 同步枚举 valueOf,降级为 DEFAULT |
| 324 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrElse { emptyList() }` — 同步 JSON 解析,降级为空列表 |
| 334 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrElse { emptyMap() }` — 同步 JSON 解析,降级为空 map |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\assistant\SillyTavernCard.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 142 | FALLBACK | `runCatching { ... }` — 同步卡片解析,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\assistant\CharacterCardExporter.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 48 | FALLBACK | `runCatching { ... }` — 同步卡片导出,降级处理 |
| 74 | FALLBACK | `runCatching { ... }` — 同步卡片导出,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\assistant\CharacterCardImporter.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 48 | FALLBACK | `runCatching { ... }` — 同步卡片导入,降级处理 |
| 63 | FALLBACK | `runCatching { ... }` — 同步卡片导入,降级处理 |
| 81 | FALLBACK | `runCatching { context.contentResolver.getType(uri) }.getOrNull() ?: ""` — 同步 MIME 类型查询,降级为空串 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\assistant\AssistantCardExporter.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 129 | FALLBACK | `runCatching { ... }` — 同步卡片解析,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\assistant\AvatarStorage.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 51 | FALLBACK | `runCatching { target.delete() }` — 同步文件删除,忽略失败 |
| 66 | FALLBACK | `?.forEach { runCatching { it.delete() } }` — 同步文件删除,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\lorebook\LorebookRepository.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 91 | FALLBACK | `runCatching { json.decodeFromString(stringListSerializer, it) }` — 同步 JSON 解析 |
| 100 | FALLBACK | `runCatching { json.encodeToString(stringListSerializer, keywords) }` — 同步 JSON 序列化 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\groupchat\GroupChatRepository.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 239 | HIGH | `runCatching { ... }` — 在 suspend 函数内,DAO 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\data\util\NetworkUtils.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 20 | FALLBACK | `runCatching { ... }` — 同步网络工具,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\ChatViewModel.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 851 | HIGH | `runCatching { sessionRepository.insertOutbox(...) }.onFailure { Logger.w(...) }` — 在 `viewModelScope.launch(Dispatchers.IO)` 内,`sessionRepository.insertOutbox` 是 suspend 调用 |
| 883 | HIGH | `runCatching { sessionRepository.deleteOutbox(outboxId) }` — 在 `viewModelScope.launch(Dispatchers.IO)` 内,`sessionRepository.deleteOutbox` 是 suspend 调用 |
| 1205 | FALLBACK | `runCatching { Uuid.parse(id) }.getOrNull()` — 同步 UUID 解析,失败返回 null |
| 1252 | HIGH | `runCatching { sessionRepository.deleteOutbox(req.outboxId) }` — 在 suspend 协程内,`deleteOutbox` 是 suspend |
| 1272 | HIGH | `runCatching { sessionRepository.deleteOutbox(req.outboxId) }` — 在 suspend 协程内,`deleteOutbox` 是 suspend |
| 1278 | HIGH | `runCatching { sessionRepository.deleteOutbox(req.outboxId) }` — 在 suspend 协程内,`deleteOutbox` 是 suspend |
| 1285 | HIGH | `runCatching { sessionRepository.deleteOutbox(req.outboxId) }` — 在 suspend 协程内,`deleteOutbox` 是 suspend |
| 1371 | FALLBACK | `runCatching { idListJson.decodeFromString<List<String>>(req.imageBase64Json) }.getOrDefault(emptyList())` — 同步 JSON 解析,降级为空列表 |
| 1379 | FALLBACK | `runCatching { Uuid.parse(req.userMessageId) }.getOrElse { Uuid.random() }` — 同步 UUID 解析,降级为随机 UUID |
| 1739 | FALLBACK | `runCatching { TokenEstimator.estimate(msgsSnapshot, sysPromptSnapshot) }.onFailure { ... }.getOrDefault(0)` — 在 suspend `withContext(Dispatchers.Default)` 内但块内纯同步 |
| 2967 | HIGH | `runCatching { toolRegistry.execute(name, args) }.getOrElse { ... }` — 在 `suspend fun callTool` 内,`toolRegistry.execute` 可能是 suspend |
| 3050 | HIGH | `runCatching { sessionRepository.deleteMessage(last.id.toString()) }` — `deleteMessage` 是 suspend 调用 |
| 3349 | FALLBACK | `runCatching { ChatGenerationService.start(appContext) }.onFailure { Logger.w(...) }` — 同步启动前台服务,失败仅记日志 |
| 3411 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 3416 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |
| 3494 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 3508 | FALLBACK | `runCatching { ReasoningLevel.valueOf(it.reasoningLevel) }` — 同步枚举 valueOf |
| 3517 | FALLBACK | `runCatching { settings.experimentsCache }` — 同步配置读取 |
| 3653 | FALLBACK | `runCatching { assistantRepository.mergeRagConfigOverride(it, ragConfig) }` — 同步配置合并(非 suspend) |
| 3752 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 3760 | FALLBACK | `runCatching { idListJson.decodeFromString<List<String>>(ast.skillIdsJson) }.getOrNull()` — 同步 JSON 解析 |
| 4135 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 4577 | HIGH | `runCatching { ... transformerPipeline.applyOnGenerationFinish(...) ...; sessionRepository.upsertMessage(...) }` — 在 suspend 上下文内,`upsertMessage` 是 suspend |
| 4588 | HIGH | `runCatching { sessionRepository.upsertMessage(sessionId, newAssistant) }.onFailure { ... }` — `upsertMessage` 是 suspend 调用 |
| 4598 | HIGH | `runCatching { triggerAutoCompress(sessionId) }` — `triggerAutoCompress` 是 suspend 调用 |
| 4642 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 4671 | FALLBACK | `runCatching { ChatGenerationService.stop(appContext) }` — 同步停止前台服务 |
| 4692 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 4742 | FALLBACK | `runCatching { idListJson.decodeFromString<List<String>>(ast.skillIdsJson) }.getOrNull()` — 同步 JSON 解析 |
| 5658 | HIGH | `runCatching { settings.saveMediaConfig(newConfig) }` — `saveMediaConfig` 是 suspend 调用 |
| 5727 | FALLBACK | `runCatching { idListJson.decodeFromString<List<String>>(json) }` — 同步 JSON 解析 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\ChatListScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 165 | FALLBACK | `runCatching { greetingFacts = factDao.getAll().take(100) }` — 同步 DAO 查询(factDao.getAll 非 suspend),忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\MemoryViewModel.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 380 | FALLBACK | `runCatching { Instant.parse(f.createdAt).isAfter(weekAgo) }.getOrDefault(false)` — 同步时间戳解析,降级为 false |
| 383 | FALLBACK | `runCatching { Instant.parse(f.createdAt).isAfter(monthAgo) }.getOrDefault(false)` — 同步时间戳解析,降级为 false |
| 395 | FALLBACK | `runCatching { ... }` — 同步解析,降级处理 |
| 785 | FALLBACK | `runCatching { ... }` — 同步 JSON 解析,降级处理 |
| 788 | FALLBACK | `runCatching { ... }` — 同步时间戳解析,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\AssistantDetailPages.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 211 | FALLBACK | `runCatching { ... }` — 同步解析,降级处理 |
| 494 | FALLBACK | `runCatching { pickAvatarLauncher.launch("image/*") }` — 同步 launcher 启动,忽略失败 |
| 1661 | FALLBACK | `runCatching { Regex(it) }.isFailure` — 同步正则编译,失败降级 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\AssistantScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 140 | FALLBACK | `runCatching { AssistantCardExporter.export(context, target, uri) }` — 同步卡片导出,降级处理 |
| 153 | FALLBACK | `runCatching { AssistantCardExporter.import(context, repo, it) }` — 同步卡片导入,降级处理 |
| 395 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\ChatScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 586 | FALLBACK | `runCatching { MuseToast.show(msg) }` — 同步 Toast 显示,忽略失败 |
| 808 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 834 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 849 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 2771 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\DebugScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 267 | FALLBACK | `runCatching { ... }` — 同步 URI 解析,降级处理 |
| 742 | FALLBACK | `runCatching { file.readText().take(CRASH_PREVIEW_CHARS) }.getOrNull()` — 同步文件读取,降级为 null |
| 1506 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 1533 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\HomeScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 278 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 284 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 463 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\HtmlPreviewScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 175 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\InputBar.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 1235 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 1691 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\LinkPreviewCard.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 204 | FALLBACK | `runCatching { context.startActivity(intent) }` — 同步启动 Activity,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\MemoryViewModel.kt`

(已在上方列出)

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\MessageBubble.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 503 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 734 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 1488 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\NotificationListenerScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 139 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\SafeModeScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 347 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 396 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\SettingsScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 545 | FALLBACK | `runCatching { settings.latestReleaseInfoFlow.first() }.getOrNull()` — 同步 Flow 读取(在非 suspend Composable 中?需确认) |
| 548 | FALLBACK | `runCatching { settings.latestReleaseInfoFlow.first() }.getOrNull()` — 同上 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\SkillScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 159 | FALLBACK | `runCatching { importLauncher.launch(...) }` — 同步 launcher 启动,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\VideoGenerationPage.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 87 | FALLBACK | `runCatching { VideoTaskStatus.valueOf(name) }.getOrNull()` — 同步枚举 valueOf,失败返回 null |
| 185 | FALLBACK ✅ | `runCatching { compressVideoReferenceImageToDataUri(...) }.onFailure { ... }` — 同步图片压缩,已有 onFailure → 保留 |
| 566 | FALLBACK | `runCatching { context.startActivity(intent) }` — 同步启动 Activity |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\chat\VideoAttachmentHandler.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 86 | HIGH | `runCatching { withContext(Dispatchers.IO) { ... } }` — `withContext` 是 suspend 调用 |
| 109 | FALLBACK | `runCatching { retriever.getFrameAtTime(...) }` — 同步帧提取,失败降级为 null |
| 124 | FALLBACK | `runCatching { retriever.release() }` — 同步 MediaMetadataRetriever 释放,忽略失败 |
| 162 | HIGH | `runCatching { withContext(Dispatchers.IO) { ... } }` — `withContext` 是 suspend 调用 |
| 173 | FALLBACK | `runCatching { retriever.release() }` — 同步释放,忽略失败 |
| 193 | FALLBACK | `runCatching { retriever.release() }` — 同步释放,忽略失败 |
| 205 | HIGH | `runCatching { ... }` — 在 suspend 函数内,可能包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\chat\ChatAudioCoordinator.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 141 | HIGH | `runCatching { settings.saveAsrConfig(config) }` — `saveAsrConfig` 是 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\chat\ChatTaskCardCoordinator.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 114 | HIGH | `runCatching { ... }` — 在 suspend 上下文内,工具执行 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\chat\ToolApprovalCard.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 144 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 207 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\chat\ImageGenCoordinator.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 154 | HIGH | `runCatching { ... }` — 在 suspend 上下文内,图像生成 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\groupchat\GroupChatDetailScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 574 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\knowledge\KnowledgeScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 228 | HIGH | `runCatching { ... }` — 在 suspend 上下文内,索引构建 |
| 259 | HIGH | `runCatching { dao.deleteDocWithChunks(id) }` — 在 suspend 上下文内,DAO 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\knowledge\KnowledgeBaseManagePage.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 161 | FALLBACK | `runCatching { settings.getRagConfig() }` — 同步配置读取,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\quicknotes\QuickNotesScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 141 | FALLBACK | `runCatching { ... }` — 同步 JSON 解析,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\qrcode\QrCodeDialogs.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 126 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\speech\TtsManager.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 282 | FALLBACK | `runCatching { ... }` — 同步 TTS 操作,降级处理 |
| 468 | FALLBACK ✅ | `runCatching { player.playbackParams = ...setSpeed(...) }` — 同步 MediaPlayer,已补 `.onFailure{Logger.w}` |
| 484 | FALLBACK | `runCatching { player.release() }` — 同步 MediaPlayer 释放,忽略失败 |
| 492 | FALLBACK | `runCatching { mp.release() }` — 同步 MediaPlayer 释放,忽略失败 |
| 498 | FALLBACK | `runCatching { mp.release() }` — 同步 MediaPlayer 释放,忽略失败 |
| 510 | FALLBACK ✅ | `runCatching { mp.currentPosition; updateState{...} }` — 同步 MediaPlayer,已补 `.onFailure{Logger.w}` |
| 523 | FALLBACK ✅ | `runCatching { mp.pause(); updateState{...} }` — 同步 MediaPlayer,已补 `.onFailure{Logger.w}` |
| 537 | FALLBACK ✅ | `runCatching { mp.start(); updateState{...} }` — 同步 MediaPlayer,已补 `.onFailure{Logger.w}` |
| 549 | FALLBACK ✅ | `runCatching { mp.seekTo(target); updateState{...} }` — 同步 MediaPlayer,已补 `.onFailure{Logger.w}` |
| 560 | FALLBACK ✅ | `runCatching { mp.seekTo(target.toInt()); updateState{...} }` — 同步 MediaPlayer,已补 `.onFailure{Logger.w}` |
| 634 | FALLBACK ✅ | `runCatching { mp.playbackParams = ...setSpeed(speed) }` — 同步 MediaPlayer,已补 `.onFailure{Logger.w}` |
| 873 | FALLBACK | `runCatching { cloudSynthChannel.close() }` — 同步 Channel 关闭,忽略失败 |
| 874 | FALLBACK | `runCatching { cloudPlayChannel.close() }` — 同步 Channel 关闭,忽略失败 |
| 901 | FALLBACK | `runCatching { cloudSynthChannel.close() }` — 同步 Channel 关闭,忽略失败 |
| 944 | FALLBACK ✅ | `runCatching { tts.setSpeechRate; tts.setPitch }` — 同步 TextToSpeech,已补 `.onFailure{Logger.w}` |
| 953 | FALLBACK | `runCatching { playbackJob?.join() }` — `join` 是 suspend 调用,但用于清理 |
| 956 | FALLBACK ✅ | `runCatching { tts.setSpeechRate; tts.setPitch }` — 同步 TextToSpeech,已补 `.onFailure{Logger.w}` |
| 1074 | FALLBACK | `runCatching { playbackJob?.join() }` — `join` 是 suspend 调用,但用于清理 |
| 1109 | FALLBACK | `runCatching { mp.release() }` — 同步 MediaPlayer 释放,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\speech\VoiceCloningPage.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 312 | FALLBACK ✅ | `runCatching { contentResolver.openInputStream(uri)?.use{...} }.getOrElse{...}` — 同步 ContentResolver,已有 getOrElse → 保留 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\speech\ElevenLabsVoiceCloningProvider.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 196 | FALLBACK | `runCatching { resp.body.string() }.getOrNull()` — 同步响应体读取,失败返回 null |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\schedule\ScheduledTasksScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 322 | HIGH | `runCatching { runner.executeTask(task) }` — `executeTask` 可能是 suspend 调用 |
| 492 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 643 | FALLBACK | `runCatching { CronExpression.parse(cronValue) }.getOrNull()` — 同步 Cron 解析,失败返回 null |
| 672 | FALLBACK | `runCatching { AppJson.decodeFromString(...) }.getOrElse { JsonObject(emptyMap()) }` — 同步 JSON 解析,降级为空对象 |
| 774 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\BackupSection.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 102 | HIGH | `runCatching { backupService.hasCloudBackup() }` — `hasCloudBackup` 是 suspend 调用 |
| 119 | HIGH | `runCatching { backupService.exportStreaming(context, it) }` — 在 `scope.launch` 内,`exportStreaming` 是 suspend |
| 137 | HIGH | `runCatching { backupService.import(context, it) }` — 在 `scope.launch` 内,`import` 是 suspend |
| 186 | FALLBACK | `runCatching { exportLauncher.launch(...) }.onFailure { ... }` — 同步 launcher 启动,失败仅记日志 |
| 199 | FALLBACK | `runCatching { importLauncher.launch(...) }.onFailure { ... }` — 同步 launcher 启动,失败仅记日志 |
| 293 | HIGH | `runCatching { backupService.hasCloudBackup() }` — `hasCloudBackup` 是 suspend 调用 |
| 623 | FALLBACK | `runCatching { onSave(newConfig) }` — 同步回调,忽略失败 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\ProviderSection.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 610 | FALLBACK | `runCatching { ProviderType.valueOf(it) }.getOrDefault(ProviderType.DEFAULT)` — 同步枚举 valueOf,降级为 DEFAULT |
| 782 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 889 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 977 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 1079 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 1107 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 1538 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 1696 | UNKNOWN | `runCatching { ... }` — 需检查块内是否包含 suspend 调用 |
| 3330 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 3351 | FALLBACK | `runCatching { ... }` — 同步 JSON 解析,降级处理 |
| 3395 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 3404 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\SettingsSubPages.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 191 | HIGH ✅ | `runCatching { ProviderRegistry.create(config).listModels(config) }` — `listModels` 是 suspend → 已改 `resultOf`+`when(result)` |
| 543 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 555 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |
| 776 | FALLBACK | `runCatching { context.startActivity(browserIntent) }` — 同步启动 Activity |
| 801 | FALLBACK | `runCatching { context.startActivity(intent) }.onFailure { ... }` — 同步启动 Activity |
| 823 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\WebServerSection.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 144 | HIGH | `runCatching { settings.saveWebServerConfig(...) }` — `saveWebServerConfig` 是 suspend 调用 |
| 178 | HIGH | `runCatching { settings.saveWebServerConfig(...) }` — `saveWebServerConfig` 是 suspend 调用 |
| 219 | HIGH | `runCatching { settings.saveWebServerConfig(...) }` — `saveWebServerConfig` 是 suspend 调用 |
| 283 | HIGH | `runCatching { settings.saveWebServerConfig(...) }` — `saveWebServerConfig` 是 suspend 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\McpSection.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 128 | HIGH ✅ | `runCatching { mcpRegistry.updateServer(...) }` — `updateServer` 是 suspend 调用 → 已改 `resultOf` |
| 135 | HIGH ✅ | `runCatching { mcpRegistry.reconnect(server.id) }` — `reconnect` 是 suspend 调用 → 已改 `resultOf` |
| 142 | HIGH ✅ | `runCatching { mcpRegistry.removeServer(server.id) }` — `removeServer` 是 suspend 调用 → 已改 `resultOf` |
| 266 | HIGH ✅ | `runCatching { mcpRegistry.startOAuthFlow(server.id) }` — `startOAuthFlow` 是 suspend 调用 → 已改 `resultOf` |
| 271 | FALLBACK ✅ | `runCatching { context.startActivity(...) }.onFailure { ... }` — 同步启动 Activity,已有 onFailure → 保留 |
| 285 | HIGH ✅ | `runCatching { mcpRegistry.revokeOAuth(server.id) }` — `revokeOAuth` 是 suspend 调用 → 已改 `resultOf` |
| 514 | FALLBACK | `runCatching { onAdd(config) }` — 同步回调,忽略失败 |
| 549 | HIGH ✅ | `runCatching { mcpRegistry.listAllResources() }` — `listAllResources` 是 suspend → 已改 `resultOf`+`onError` |
| 608 | FALLBACK | `runCatching { ... }` — 同步文件读取,降级处理 |
| 699 | HIGH ✅ | `runCatching { mcpRegistry.listAllPrompts() }` — `listAllPrompts` 是 suspend → 已改 `resultOf`+`onError` |
| 840 | HIGH ✅ | `runCatching { mcpRegistry.getPrompt(...) }` — `getPrompt` 是 suspend → 已改 `resultOf`+`onError` |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\ProviderPluginPage.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 122 | FALLBACK | `runCatching { if (tempFile.exists()) tempFile.delete() }` — 同步文件删除,忽略失败 |
| 146 | HIGH ✅ | `runCatching { importFromUri(uri) }` — `importFromUri` 是 suspend → 已改 `resultOf`+`onError` Logger.w |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\CloudBackupPage.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 119 | HIGH | `runCatching { cloudBackupService.listBackups(cloudConfig) }` — `listBackups` 是 suspend 调用 |
| 260 | HIGH | `runCatching { settings.saveCloudBackupConfig(draft) }` — `saveCloudBackupConfig` 是 suspend 调用 |
| 308 | HIGH | `runCatching { cloudBackupService.listBackups(cloudConfig) }.getOrDefault(emptyList())` — `listBackups` 是 suspend 调用 |
| 363 | HIGH | `runCatching { cloudBackupService.listBackups(cloudConfig) }` — `listBackups` 是 suspend 调用 |
| 431 | HIGH | `runCatching { ... }` — 在 suspend 上下文内,云备份操作 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\PresetProviderPickerDialog.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 475 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\settings\AuditLogPage.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 681 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\navigation\ChatNavGraph.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 291 | FALLBACK | `runCatching { ... }` — 同步 HTML 读取,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\ui\workflow\WorkflowEditorViewModel.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| 306 | HIGH ✅ | `runCatching { ...settingsRepository.updateMultiAgentConfig{...} }` — `updateMultiAgentConfig` 是 suspend → 已改 `resultOf`+`onError` |
| 344 | FALLBACK ✅ | `runCatching { AppJson.decodeFromString(...) }.onFailure{...}` — 同步 JSON 解析,已有 onFailure → 保留 |
| 375 | FALLBACK ✅ | `runCatching { workflowFile.readText(); decodeFromString }.onFailure{Logger.w}` — 同步文件读取,已有 onFailure → 保留 |
| 395 | FALLBACK | `runCatching { ... }` — 同步操作,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\license\LicensesScreen.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | FALLBACK | (具体行号需确认)同步许可证读取,降级处理 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\asr\DashScopeAsrClient.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | HIGH | (具体行号需确认)在 suspend 函数内,ASR HTTP 调用 |

### `e:\1Project\Muse\1muse\app\src\main\java\io\zer0\muse\web\WebServer.kt`

| 行号 | 类别 | 上下文摘要 |
|------|------|-----------|
| — | ✅ 已全部转换 | 此前已全部改为 `resultOf`(L135/L163/L218),无剩余 `runCatching` 调用;grep 命中仅为注释 |

---

## 统计表

> **处理进度 (2026-07-28 最终)**: Phase 3.3+3.4 完成。
> - 第一批: 21 处 UNKNOWN (6→HIGH / 14→FALLBACK / 1 WebServer 已转换)
> - 第二批 (本次): 剩余 31 处 UNKNOWN 全部归类为 FALLBACK (块内均为纯同步逻辑,无 suspend 调用)
>   - 其中 5 处实际已迁移 resultOf (inventory 行级状态过期): MessageBubble L734 / ProviderSection L782/L889/L977/L1079
>   - 其中 5 处补 `.onFailure { Logger.w }` 日志: ChatViewModel L3415/L3498/L4139/L4696 (notification) + L3756 (toolIdsJson 解析)
> - **UNKNOWN 归零。Phase 3.1-3.4 全部完成,剩余 Phase 3.5 全量回归测试 (需 Android SDK 环境)。**

### 按模块统计

| 模块 | HIGH | FALLBACK | THROW | UNKNOWN | 合计 |
|------|------|----------|-------|---------|------|
| :ai | 6 | 18 | 0 | 0 | 24 |
| :memory | 4 | 31 | 0 | 0 | 35 |
| :app | 44 | 129 | 0 | 0 | 173 |
| :common | 0 | 0 | 0 | 0 | 0 |
| :material3 | 0 | 0 | 0 | 0 | 0 |
| **合计** | **54** | **178** | **0** | **0** | **232** |

### 按类别统计

| 类别 | 数量 | 占比 | 迁移建议 | 状态 |
|------|------|------|---------|------|
| HIGH | 54 | 23.3% | 改 `resultOf {}.onSuccess{}.onError{}` (suspend 调用类) | ✅ 全部迁移完成 |
| FALLBACK | 178 | 76.7% | 保留 `runCatching`,补 `.onFailure{Logger.w}` 日志 | ⚠️ HIGH 批已迁移;FALLBACK 批关键文件已补日志,剩余按需补 |
| THROW | 0 | 0% | 改 `try/catch` 显式传递 | N/A |
| UNKNOWN | 0 | 0% | 人工 review 后归入 HIGH/FALLBACK/THROW | ✅ 归零 |

### 按文件统计(前 10 名)

| 文件 | runCatching 数量 |
|------|------------------|
| ChatViewModel.kt | 31 |
| TtsManager.kt | 19 |
| MemoryFileWriter.kt | 15 |
| WebSearchService.kt | 15 |
| McpClient.kt | 13 |
| DocumentParser.kt | 10 |
| GroupChatScheduler.kt | 11 |
| ProviderSection.kt | 12 |
| McpSection.kt | 11 |
| AssistantRepository.kt | 6 |

### 高危文件统计(HIGH 数量 ≥ 2)

| 文件 | HIGH 数量 | 主要风险点 |
|------|-----------|-----------|
| ChatViewModel.kt | 9 | outbox 写入/删除、upsertMessage、triggerAutoCompress、toolRegistry.execute |
| WebServerSection.kt | 4 | settings.saveWebServerConfig (suspend) |
| BackupSection.kt | 3 | backupService.hasCloudBackup/import/exportStreaming (suspend) |
| CloudBackupPage.kt | 5 | cloudBackupService.listBackups / settings.saveCloudBackupConfig (suspend) |
| McpSection.kt | 4 | mcpRegistry.updateServer/reconnect/removeServer/startOAuthFlow (suspend) |
| FactStore.kt | 4 | dao.searchFts/countFacts/countFts/insertFts (suspend DAO) |
| KlingVideoProvider.kt | 2 | exec(httpRequest) (suspend) |
| GenericOpenAiVideoProvider.kt | 2 | exec(httpRequest) (suspend) |
| AgnesVideoProvider.kt | 2 | exec(httpRequest) (suspend) |
| VideoGenerationService.kt | 2 | provider.submit (suspend) |
| VideoAttachmentHandler.kt | 3 | withContext(Dispatchers.IO) (suspend) |
| FolderRepository.kt | 2 | folderDao.incrementSessionCount (suspend DAO) |
| KnowledgeScreen.kt | 2 | 索引构建 + dao.deleteDocWithChunks (suspend) |

### 迁移优先级建议

1. **P0 紧急(HIGH 中最危险)**: ChatViewModel.kt 的 9 处 suspend 调用被 runCatching 包裹(与 P0-1.4 同类)
2. **P1 高优**: FactStore.kt 的 4 处 suspend DAO 调用(项目已明确政策 H5: 用 resultOf)
3. **P2 中优**: Provider/Service 层的 suspend 调用(Kling/GenericOpenAi/AgnesVideoProvider、WebServerSection、McpSection、CloudBackupPage)
4. **P3 低优**: FALLBACK 类(145 处)按文件批量补 `catch (CancellationException) { throw e }`
5. **P4 待定**: UNKNOWN 类(41 处)需人工 review

---

## 备注

1. **THROW 类为 0**: 项目中没有发现 `getOrThrow()` 或 `getOrNull()` 后立即 throw/return error 的模式,所有错误传递已用 `resultOf {}` + `onError {}` 处理(项目已有 93 文件 653 处使用 `resultOf`)。

2. **UNKNOWN 类说明**: 主要集中在大型 UI 文件(ChatScreen.kt、ProviderSection.kt、McpSection.kt 等)中,这些文件的 `runCatching` 块体较长,需读取更多上下文才能判断块内是否包含 suspend 调用。建议在 Phase 3.1 扫描分类时优先处理这些文件。

3. **分类策略**: 本清单采用"块内是否调用 suspend 函数"作为 HIGH 的主要判据,而非"所在函数是否为 suspend fun"。理由:`runCatching` 是 inline 函数,纯同步块无 suspend 挂起点,不会触发 `CancellationException`。仅当块内确实调用 suspend 函数时才有吞 `CancellationException` 风险。

4. **未列出文件**: 部分文件(如 `WebServer.kt`、`LicensesScreen.kt`、`DashScopeAsrClient.kt`、`EmbeddingService.kt`、`EmbeddingProvider.kt`、`UpdateChecker.kt`、`SecureKeyStore.kt`、`TokenEstimator.kt`、`ContextCompressTransformer.kt`、`MemoryInjectionTransformer.kt`)因 grep 结果中 `runCatching` 出现在注释中或文件较大未完整读取,标"—"表示需后续补全具体行号。

5. **遗留风险 (TtsManager.kt L953/L1074)**: `runCatching { playbackJob?.join() }` 中 `join()` 是 suspend 调用,理论上会吞 `CancellationException`。这两处不在本批处理清单中(原标 FALLBACK "用于清理"),但建议后续 review 时确认是否需改 `resultOf`。

6. **WebServer.kt 已确认完成**: 该文件 3 处 `runCatching` 已在早期全部改为 `resultOf`(L135/L163/L218),grep 命中的 3 处仅为注释 `// M4/M5/M6: runCatching 改为 resultOf`。
