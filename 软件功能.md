# Muse 使用说明书

> 目标：让你第一次打开 Muse 就能用起来。所有操作都写在里面，包括那些"藏起来的"手势。
> 看不懂的词？跳过就行，不影响使用。

---

## 目录

1. [安装与首次使用](#1-安装与首次使用)
2. [认识聊天界面](#2-认识聊天界面)
3. [发消息的十八般武艺](#3-发消息的十八般武艺)
4. [消息操作：点、滑、长按](#4-消息操作点滑长按)
5. [会话管理](#5-会话管理)
6. [换一个更聪明的模型](#6-换一个更聪明的模型)
7. [它会记住你](#7-它会记住你)
8. [让它帮你干活](#8-让它帮你干活)
9. [AI 上网时，你能看到它在干嘛](#9-ai-上网时你能看到它在干嘛)
10. [它写的代码、文档会变成卡片](#10-它写的代码文档会变成卡片)
11. [多几个助手一起用](#11-多几个助手一起用)
12. [群聊](#12-群聊)
13. [把外观调成你喜欢的样子](#13-把外观调成你喜欢的样子)
14. [安全与隐私](#14-安全与隐私)
15. [备份，别怕换手机](#15-备份别怕换手机)
16. [进阶玩法](#16-进阶玩法)
17. [常见问题](#17-常见问题)

---

## 1. 安装与首次使用

### 安装

- 手机需要 **Android 8.0 或以上**。
- 到官网 [museai.ltd](https://museai.ltd) 或 GitHub Releases 页面下载安装包。
- 选哪个包？
  - 最近两年的手机 → **arm64** 版
  - 很老的手机 → **armv7** 版
  - 不确定 → **universal** 通用版
- 安装时提示"未知来源"→ 点允许，这是正常的。

### 首次设置（跟着引导走）

1. 填你的名字，给它起个名字。
2. 选一个模型服务商（DeepSeek、通义千问、智谱等，注册都送免费额度），填入 API Key。
3. 选一个模型，随便选，之后随时能换。

---

## 2. 认识聊天界面

一个对话页从上到下分三块：

- **顶部**：左边返回按钮，中间是对话标题（点击可切换对话），右边是模型胶囊和压缩按钮。
- **中间**：消息列表，你和 AI 的对话都在这里。
- **底部**：输入栏（打字框、加号、语音按钮、发送按钮）。

顶部中间那个标题，藏着两个操作：

- **单击标题** → 弹出会话列表，切换对话。
- **长按标题** → 弹出助手面板，切换"人"（换一个助手来聊，后面第 11 章细说）。

---

## 3. 发消息的十八般武艺

### 打字发送

输入框打字 → 点发送。AI 回复会一个字一个字蹦出来，不用等。

### 说而不打（语音输入）

- **长按输入框旁边的麦克风**，对着手机说话，松手发送。
- 它边说边转字，你可以看着文字确认它没听错。
- 录音时**手指上滑** = 取消发送（反悔专用）。

### 发图片

- 点输入框的 **加号**，选"图片"，从相册挑或直接拍照。
- AI 能看懂图：截图、表格、表情包、照片都行，一次可以发多张。
- 发图后可以顺便问"图里写了什么？""帮我看看这张表"。

### 发文档

- 同样从加号里选"文档"，支持 Word、PDF、TXT 等常见格式。
- AI 会直接读取文档内容（扫描件、图片型 PDF 也能认字）。

### 斜杠命令

- 输入框里打 `/`，会弹出命令列表：
  - `/new` 新开对话
  - `/compact` 压缩当前对话（太长了之后用它）
  - `/reset` 重置对话
  - `/pin` 置顶当前消息
  - `/archive` 归档当前对话
- 记不住命令没关系，打出 `/` 看提示就行。

### 全屏输入（长文专用）

- **长按输入框的空白处**，进入全屏输入模式。写长文、复制粘贴大段内容更舒服，写完全屏右下角发送。

### 加号里的其他东西

- 快捷操作栏展开后还有：新建任务、全局搜索等入口。
- **长按加号**可以展开 / 收起这一排按钮。嫌界面乱就收起来，再长按一次展开。

### 发消息前的状态提示

- 输入框上方如果出现"上次有 N 个工具调用未完成"的提示条，说明上次对话中途被打断过，可以点"恢复执行"接着跑，或"丢弃"清掉。

---

## 4. 消息操作：点、滑、长按

### 重新生成与版本

- 每条 AI 回复下方有**重新生成**按钮，点它换个说法再写。
- 重新生成几次后，消息右上角出现 **"1/3"** 之类的标签，点它可在各版本间切换。
- 想固定某一版，切换到那版后继续聊即可，其他版本不会丢，随时能切回。

### 消息手势（藏起来的操作）

- **左滑一条消息** → 快速引用它（引用后发送，AI 会结合那条消息回答）。
- **右滑一条消息** → 直接进入编辑状态（改你自己的消息，重新发送）。
- **长按一条消息** → 弹出完整操作菜单：
  - 复制（纯文本 / Markdown 格式）
  - 转发到其他会话
  - 分享到微信、QQ 等
  - 收藏（进收藏夹；收藏页里**长按消息**可以设置分组标签，方便归类）
  - 编辑、重新生成、翻译、朗读
  - 删除（可以只删这条，或连同后续一起删）
  - 从这里分支（见下）

### 分支（对话分叉）

- 长按某条**你自己的消息** → 选择"从此处分支"。
- 效果：从这条消息开始走出另一条对话线，原对话原封不动保留。
- 适合"如果当时我换个问法会怎样"的探索。

### 引用回复

- 消息被引用后，发送时输入框上方会显示引用来源。
- AI 回复时会看到你引用的原文，回答更有针对性。

### Mood（AI 的内心独白）

- 有些 AI 回复顶部有一个折叠的 **Mood 卡片**，这是 AI 回复前的心理活动：当时的情绪、闪过的念头、对自己的反思。
- 默认折叠不打扰，**点一下展开**看看它在想什么，挺有意思。

### 任务卡

- AI 调用工具（查天气、算数等）时，对话里会出现一张**任务卡**，分步列出：调了什么 → 参数是什么 → 结果如何 → 成没成功。
- 点卡片可展开看详情；失败的工具步骤会自动展开显示错误。

---

## 5. 会话管理

### 新建与切换

- 左上角菜单 / 会话列表页，点 **+** 新建对话。
- 对话页顶部**单击标题**切换会话。
- 每个会话是独立的上下文：这个对话里聊猫，另一个对话里聊工作，互不串。

### 会话列表里的手势

- **右滑会话** → 归档（眼不见为净，不删除）。
- **左滑会话** → 删除（列表底部有撤销提示，手滑了能救回）。
- **长按会话** → 菜单：置顶、归档、重命名、移动到文件夹、删除。
- **长按文件夹** → 编辑文件夹（重命名、删文件夹）。
- **置顶会话拖动重排**：长按置顶区条目右侧的拖动手柄，上下拖动调整顺序。

### 全局搜索

- 会话列表页顶部有搜索框，可以搜聊天内容（全文搜索，搜关键词就能定位到哪条消息）。
- 搜索页还能按收藏、按标签筛选。

### 会话过期与主动消息

- 太久没聊的会话不会自动删，放心。
- 相反，如果开了"主动消息"（见第 16 章），AI 还会主动来找你。

---

## 6. 换一个更聪明的模型

- 对话页顶部显示当前模型名，**点击模型胶囊**弹出模型列表。
- 40+ 服务商随便换：DeepSeek、GPT、Claude、Gemini、通义千问、智谱、豆包……
- 列表里模型旁有标签：会看图、会深度思考、会上网，按需选。
- 同一段对话中途换模型没问题，它不会失忆。

### 深度思考

- 输入框上方有个"深度思考"开关（或对话设置里），打开后 AI 会多花时间多想一步，回答复杂问题（写代码、推理、长文）质量更高，代价是慢一点。
- 思考等级可以调：低 / 中 / 高。

---

## 7. 它会记住你

这是 Muse 和其他聊天软件最大的区别：**它真的记得你。**

- 你随口说过"我养了只叫年糕的猫"、"我不吃香菜"，下次聊天它都记得。
- 重要的（姓名、职业、过敏）永远不忘；不重要的（昨天想吃什么）慢慢淡忘，跟人一样。
- **你可以管它记什么**：设置 → 记忆。
  - 看到它记的所有条目，记错了就删。
  - 觉得重要的手动加一条。
  - 可以标记"永远不要忘"（关键信息不衰减）。
  - 能按时间、按重要程度筛选，还能搜索。
- 记忆**只存在手机里**，不上传任何服务器。

### 经验库

- 设置 → 记忆 → 经验库：可以把"这类问题以后都这么处理"的方法沉淀下来，AI 遇到同类问题会参考。
- 适合你反复纠正它的场景（比如"以后提到我妹就说'小满'"）。

---

## 8. 让它帮你干活

直接吩咐就行："帮我算一下 23×47""查一下今天成都天气""下午 3 点提醒我喝水""搜一下这个问题的答案""把这段翻译成英文""读出来""生成一张猫的图片"。

它会自动调用对应工具，并在对话里用**任务卡**展示过程。

### 它能调用的工具（大概）

- 算数、查天气、看时间
- 联网搜索、读网页
- 定时提醒、日历
- 剪贴板（读 / 写）
- 短信、电话、闹钟（需要你授权）
- 打开 App、打开设置页
- 生成图片
- 读文件、写文件（应用自己的空间里）
- 更多技能可通过插件扩展

### 审批：做有影响的事之前，它会问你

工具分"安全"和"有影响"两类。有影响的（发短信、设闹钟、装东西等），调用前弹一张小卡片：

- **允许**：这次同意。
- **取消**：拒绝。
- **本会话允许**：这次同意，并且当前对话里以后不再问这个工具。
- **始终允许**：以后永远不再问这个工具。

三种大模式（设置 → 聊天设置 → 工具批准）：

- **完全放权**：什么都不问，直接干。（适合信得过的环境）
- **按需批准**（推荐）：安全的小事自动干，有影响的问一下。
- **严格模式**：几乎所有工具都要问。

如果它连续几次干活失败，会停下来告诉你原因，不会硬撑。

---

## 9. AI 上网时，你能看到它在干嘛

Muse 内置了一个隐形浏览器，AI 查资料、看网页、填表单都靠它。

- AI 上网时，对话页顶部出现一个**小胶囊**，显示它正在访问的网页标题。
- **点一下胶囊** → 全屏实时直播它浏览的页面，看它在干嘛。
- 全屏页顶部按钮：
  - **返回**：收起直播，它继续在后台干活（胶囊还在，随时再看）。
  - **刷新**：重新加载当前页面。
  - **关闭浏览器**（叉号）：彻底关掉，胶囊也会消失。
- 每个对话有**独立浏览器**：这个对话关掉不影响另一个对话的页面。

---

## 10. 它写的代码、文档会变成卡片

当 AI 回复里有一段"值得单独拿出来"的内容（代码、文章、表格、网页），它会自动保存成**产物卡片**显示在消息下方。

- 卡片有标题，点开全屏看完整内容，方便复制、保存、反复看。
- 想让它把某段内容单独成卡，直接说"把这段放到卡片里"。

---

## 11. 多几个助手一起用

### 内置与自定义

- **预设助手**：内置现成的角色（写作、代码、闲聊等），在"助手"页直接启用。
- **自定义助手**：设置 → 助手 → 新建。给它起名、设定性格、说话风格。
  - 例："毒舌损友，说话直接不客气"；"温柔学姐，像朋友一样聊天"。
  - 还能指定它用哪个模型、说什么语言、用什么语气。

### 切换助手（两种方式）

- 对话页顶部**长按标题** → 弹出助手列表，直接换人聊。
- 或对话页右上角模型胶囊旁边，切换到"助手"模式。

### 让助手们配合

- 输入框打 `@助手名 帮我写个开头`，它会派活儿给对应助手，任务卡里能看到委派过程。
- 每个助手有独立记忆，不会串味。

### 助手的进阶设置

- **资源关联**：可以给助手绑定一个知识库（它回答时优先查你给的资料）。
- **世界书**：给助手设定世界观（适合写小说、跑团），它会在合适的时机引用设定。
- **导入导出**：设置好的助手可以打包成文件分享给朋友，也能导入别人的。

### 助手页的手势

- 长按助手卡片 → 操作菜单（编辑、启用/停用、导出、删除）。

---

## 12. 群聊

把几个助手拉进同一个会话，让它们像真人一样讨论：

- 创建群聊：会话列表 → 群聊 → 新建，勾选要拉进来的助手。
- 发言模式可选：**顺序发言**（轮流来）或**自由轮转**（谁想接话谁接）。
- 讨论时界面显示当前谁在思考、谁在等。
- 不想让它插话，可以在它发言前跳过本轮。
- 适合头脑风暴、多角色讨论、模拟圆桌会议。
- 每个助手在群聊里保持自己的记忆，不污染主对话。

---

## 13. 把外观调成你喜欢的样子

- **主题**：设置 → 外观。12 套内置主题（暖纸、樱花、海洋、墨、暮紫……），每套有白天/夜间两版。
- **定时切换**：可以设置"白天用亮色、晚上自动变暗"。
- **动态取色**：从壁纸提取颜色生成主题（安卓 12+）。
- **自定义主题**：自己调颜色，还可以选色盲友好配色。
- **字体**：换字体、调字号。
- **消息气泡圆角**：可调方/圆。

---

## 14. 安全与隐私

- **应用锁**：设置 → 安全，开 PIN 锁或指纹解锁。防别人翻手机。
  - 连续输错 5 次会锁 30 秒。
- **隐私保护**：聊天里提到的手机号、身份证、银行卡，默认自动打码（AI 看到的是替身，真实号码不会发给 AI 服务商）。
- **数据都在本地**：聊天、记忆、知识库全部存在手机里。不注册、不登录、不上传。
- **联网功能默认关闭**，需要时手动开（搜索、网页等）。
- **链接二次确认**：点消息里的链接会先问"确定打开吗"，防误触。
- **崩溃日志**：只存本地，不会自动上传。

---

## 15. 备份，别怕换手机

设置 → 备份与恢复：

- **本地备份**：一键打包全部数据（聊天、记忆、设置）。
- **网盘备份**：配置 WebDAV 或 S3（坚果云、Nextcloud 等都支持），自动定期上传。
- 备份文件**加密**，只有你的密码能解开。
- 换手机：新手机装 Muse → 恢复备份 → 全部回来。

---

## 16. 进阶玩法

- **通知监听**：设置里开启后，AI 能读手机通知。快递到了、有人发消息，它能接话。
- **主动找你聊天**：设置"多久没说话就主动来聊"，可调间隔和时段，像真朋友。
- **网页版**：设置 → 网页服务器，开启后同一 WiFi 下用电脑浏览器访问，浏览对话和设置。
  - 首次访问输 PIN 码（设置里能看到），别人连不上。
- **表情包库**：导入表情包压缩包，AI 聊天时会按气氛发。
- **桌面小部件**：桌面放 Muse 小部件，一键新建对话。
- **长按应用图标**：快捷菜单（新建会话、打开设置等）。
- **翻译**：消息长按菜单里有"翻译"，直接翻译成别的语言；设置里还能调翻译用的模型。
- **朗读**：消息长按菜单里"朗读"，AI 把回复读出来（系统或云端语音）。
- **技能插件**：设置 → 工具，可以导入技能包给 AI 加自定义能力（不用会写代码）。
- **定时任务**：可以让 AI 在指定时间执行任务（如每天早上的日报）。

---

## 17. 常见问题

**问：它老说"需要 API Key"，是什么？**
答：AI 服务的"门票"。去预置服务商（DeepSeek、通义千问等）官网注册，找到"API 密钥"页面复制一串字符，粘到 设置 → 模型服务。注册基本都送免费额度。

**问：聊天记录存哪里？会被别人看到吗？**
答：存在你手机里，不上传。不注册、不登录。

**问：换了模型，它还记得之前聊的吗？**
答：记得。记忆在 Muse 这边，不在模型那边。

**问：为什么它有时候半天不回话？**
答：可能在深度思考或上网查资料。看看有没有任务卡——有就是在干活。太久没动静点停止，重新问。

**问：免费版能用吗？**
答：Muse 完全免费开源。唯一可能花钱的是 AI 服务商的 API 费用（按量计费很便宜，也有免费额度可用）。Muse 不抽成、不收费。

**问：怎么更新到最新版？**
答：官网 museai.ltd 或 GitHub Releases。覆盖安装即可，数据不丢。

**问：换手机数据怎么搬？**
答：旧手机备份 → 新手机恢复（见第 15 章），全程加密。

**问：字太小 / 太亮 / 想要别的颜色？**
答：设置 → 外观，主题、字号、夜间模式都能调。

---

*Muse 是开源软件，代码完全公开。遇到问题欢迎进交流群（GitHub 主页有二维码）找作者和其他用户聊聊。*
